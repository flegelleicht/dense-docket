    <tr>
      <td><%= link_to event.title,  "/plans/#{event.plan.id}/events/#{event.id}/edit" %></td>
      <td><%= event.start %></td>
      <td><%= event.end %></td>
      <td><%= event.lead_days%></td>
      <td><%= link_to 'Remove', [event.plan, event], 
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
